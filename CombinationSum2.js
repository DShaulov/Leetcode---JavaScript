/**
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
var combinationSum2 = function(candidates, target) {
    const combinations = [];
    const permutations = [];
    permutations.push([candidates[0]]);
    for (let i = 1; i < candidates.length; i++) {
        const ithLevelPermutation = [];
        
    }
};