/**
 * @param {number[]} nums
 * @return {string[]}
 */
var summaryRanges = function(nums) {
    const output = [];
    let i = 0;
    while(i < nums.length) {
        for (let j = i; j < nums.length; j++) {
            if (nums[j] != nums[j - 1] + 1) {

            }
        }
    }
};

/**
 * Edge cases:
 *  1. Final number 
 */